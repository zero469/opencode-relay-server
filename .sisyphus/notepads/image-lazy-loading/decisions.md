# Decisions

## 2026-02-17 Session Start
- URL replacement format: `lazy:{partID}` for app detection
- Strip only `data:image/*` URLs (preserve PDFs, external URLs)
- Memory cache only (no localStorage persistence)

